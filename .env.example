# Required runtime environment variables
ANTHROPIC_API_KEY=your_anthropic_api_key
CHAT_MODEL=claude-sonnet-4-5
CHAT_REASONING_MODE=full
CHAT_REASONING_BUDGET_TOKENS=1024
CHAT_COMPACTION_TRIGGER_TOKENS=120000

EXA_API_KEY=your_exa_api_key
SEMANTIC_SCHOLAR_API_KEY=your_semantic_scholar_api_key
OPENALEX_EMAIL=you@example.com
UNPAYWALL_EMAIL=you@example.com

NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SPACETIMEDB_URI=wss://maincloud.spacetimedb.com
NEXT_PUBLIC_SPACETIMEDB_DATABASE=rabbit-hole-db

# Cloudflare R2 — required for graph persistence (save/load per rabbit hole)
# Without these the graph is in-memory only and not persisted between sessions.
# Create a bucket in the Cloudflare R2 dashboard, then generate an API token
# with Object Read & Write permissions scoped to that bucket.
# Both naming conventions are accepted (CLOUDFLARE_R2_* preferred):
CLOUDFLARE_R2_ACCOUNT_ID=your_cloudflare_account_id    # also: CLOUDFLARE_ACCOUNT_ID
CLOUDFLARE_R2_ACCESS_KEY_ID=your_r2_access_key_id      # also: R2_ACCESS_KEY_ID
CLOUDFLARE_R2_SECRET_ACCESS_KEY=your_r2_secret_key     # also: R2_SECRET_ACCESS_KEY
CLOUDFLARE_R2_BUCKET=your_r2_bucket_name

# Auth0 — required for user authentication
# Create a Regular Web Application in the Auth0 dashboard.
# Set Allowed Callback URLs to: http://localhost:3000/auth/callback
# Set Allowed Logout URLs to: http://localhost:3000
AUTH0_SECRET=your_auth0_secret_32_bytes_hex   # openssl rand -hex 32
AUTH0_DOMAIN=your-tenant.us.auth0.com
AUTH0_CLIENT_ID=your_auth0_client_id
AUTH0_CLIENT_SECRET=your_auth0_client_secret
APP_BASE_URL=http://localhost:3000

# Optional
# OPENAI_API_KEY=your_openai_api_key
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
